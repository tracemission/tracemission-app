import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

const CurrentTracking = () => {
    return (
        <Text>Now in CurrentTracking</Text>
    );
}

export default CurrentTracking;